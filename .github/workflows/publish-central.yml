name: Publish to Maven Central

on:
  push:
    tags: ['v*.*.*']   # push a tag like v1.2.3 to publish

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'
          server-id: 'central'
          server-username: ${{ secrets.CENTRAL_USERNAME }}
          server-password: ${{ secrets.CENTRAL_PASSWORD }}
          # This imports your ASCII-armored private key into the GPG keyring used by Maven
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase:  ${{ secrets.GPG_PASSPHRASE }}
        

      - name: Build, sign, and publish
        run: mvn -e -X -B -ntp -DskipTests -P production,release deploy
        env:
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          MAVEN_GPG_KEYNAME: ${{ secrets.GPG_KEYNAME }}
          