#!/usr/bin/env kscript

println("Greetings!")
println('This script is executed through the command "kscript".')
println("You can execute all kinds of script through using kscript and putting different params into the script. These params are defined in the job.")
println("Through these Kotlin scripts, you are able to access a lot of powerful things in the application, such as the Spring application context.")
println("If Kotlin isn't your language of choice, we also support JavaScript, shell-scripts, SQL and PHP.")